import 'package:flutter/material.dart';

class ScrollHelperScreen extends StatelessWidget {
  const ScrollHelperScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.transparent);
  }
}
